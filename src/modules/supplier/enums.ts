export enum SupplierWeighting {
    avgQuoteResponse,
    quoteToCostRatio,
    rating,
    greenScore,
}
export enum SupplierPermission {
    unknown,
    canReceiveQuoteInvite,
    canProvideQuotation,
  }